import 'package:flutter/material.dart';

class ChatMessageLayout extends StatelessWidget {
  const ChatMessageLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
